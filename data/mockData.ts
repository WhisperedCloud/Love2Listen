import { Playlist } from '../types';

// The application will start with an empty library.
// Users can add their own songs by clicking the '+' icon in the sidebar.
export const playlists: Playlist[] = [];
